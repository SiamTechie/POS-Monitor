# 📦 Windows Installer Guide

## 🎯 ภาพรวม

คู่มือนี้จะแนะนำวิธีสร้าง Windows Installer (.exe) สำหรับ POS Monitor

---

## 📋 สิ่งที่ต้องเตรียม

### 1. ติดตั้ง Inno Setup

1. ดาวน์โหลด: https://jrsoftware.org/isdl.php
2. ดาวน์โหลด **Inno Setup 6.x**
3. ติดตั้งตามปกติ

---

## 🏗️ วิธีสร้าง Installer

### ขั้นตอนที่ 1: Build EXE

```cmd
cd d:\Project\monitor-net\python-monitor
pyinstaller --onefile --noconsole --name "POS-Monitor" monitor-rest-api.py --clean
```

### ขั้นตอนที่ 2: เปิด Inno Setup

1. เปิดโปรแกรม **Inno Setup Compiler**
2. File > Open > เลือก `installer.iss`
3. Build > Compile

### ขั้นตอนที่ 3: รอให้ Build เสร็จ

ไฟล์ Installer จะอยู่ที่:
```
python-monitor\installer-output\POS-Monitor-Setup.exe
```

---

## ✨ คุณสมบัติของ Installer

### 1. **ง่ายต่อการใช้งาน**
- ✅ Next > Next > Finish
- ✅ ถามชื่อสาขาตอนติดตั้ง
- ✅ เลือก Auto-start ได้

### 2. **ติดตั้งอัตโนมัติ**
- ✅ คัดลอกไฟล์ไปยัง `C:\Program Files\POS-Monitor`
- ✅ สร้าง config.txt พร้อมชื่อสาขา
- ✅ ตั้งค่า Auto-start (ถ้าเลือก)
- ✅ สร้าง Shortcut

### 3. **Uninstall ได้**
- ✅ หยุด POS-Monitor.exe
- ✅ ลบ Task Scheduler
- ✅ ลบไฟล์ทั้งหมด

---

## 🎯 วิธีใช้งาน Installer

### สำหรับพนักงานสาขา:

1. **Double-click** `POS-Monitor-Setup.exe`

2. **คลิก Next** ผ่านหน้าต่างต้อนรับ

3. **ใส่ชื่อสาขา**
   ```
   Branch Name: สาขาสีลม
   ```

4. **เลือก Options**
   - ✅ Start automatically when Windows starts (แนะนำ)
   - ☐ Create desktop icon (ถ้าต้องการ)

5. **คลิก Install**

6. **เสร็จ!**
   - ✅ ระบบจะเริ่มทำงานทันที
   - ✅ จะรันอัตโนมัติทุกครั้งที่เปิดเครื่อง

---

## 📊 เปรียบเทียบวิธีติดตั้ง

| วิธี | ข้อดี | ข้อเสีย |
|------|-------|---------|
| **BAT File** | ง่าย, ไม่ต้องติดตั้งอะไร | ต้องคัดลอกหลายไฟล์ |
| **Windows Installer** ⭐ | มืออาชีพ, ติดตั้งง่าย, Uninstall ได้ | ต้องมี Inno Setup |

---

## 🔧 การปรับแต่ง Installer

### แก้ไขชื่อบริษัท

เปิด `installer.iss` แก้บรรทัดที่ 6:
```
#define MyAppPublisher "Your Company"
```

### แก้ไข Firebase URL

แก้บรรทัดที่ 69:
```
ConfigContent[1] := 'FIREBASE_URL=https://your-project.firebaseio.com';
```

### เพิ่ม Icon

1. เตรียมไฟล์ `.ico`
2. แก้บรรทัดที่ 24:
```
SetupIconFile=icon.ico
```

---

## 📦 การแจกจ่าย

### วิธีที่ 1: USB Drive

1. คัดลอก `POS-Monitor-Setup.exe` ไปยัง USB
2. แจกจ่ายไปสาขา
3. ให้พนักงาน Double-click ติดตั้ง

### วิธีที่ 2: Network Share

1. วางไฟล์บน Network Drive
2. แจ้ง Path ให้พนักงาน
3. ให้พนักงานเข้าไปติดตั้ง

### วิธีที่ 3: Email/Cloud

1. Upload ไปยัง Google Drive / OneDrive
2. ส่งลิงก์ให้พนักงาน
3. ให้พนักงานดาวน์โหลดและติดตั้ง

---

## 🐛 Troubleshooting

### ปัญหา: "Inno Setup not found"

**แก้:** ติดตั้ง Inno Setup จาก https://jrsoftware.org/isdl.php

### ปัญหา: "Cannot find POS-Monitor.exe"

**แก้:** Build EXE ก่อน:
```cmd
pyinstaller --onefile --noconsole --name "POS-Monitor" monitor-rest-api.py
```

### ปัญหา: Installer ติดตั้งแล้วไม่ทำงาน

**แก้:** 
1. ตรวจสอบ `C:\Program Files\POS-Monitor\config.txt`
2. ตรวจสอบ Task Scheduler มี "POS Monitor" หรือไม่
3. รัน `C:\Program Files\POS-Monitor\POS-Monitor.exe` ด้วยตนเอง

---

## 📋 Checklist การสร้าง Installer

```
□ ติดตั้ง Inno Setup แล้ว
□ Build POS-Monitor.exe แล้ว
□ แก้ไข installer.iss (ชื่อบริษัท, Firebase URL)
□ Compile ด้วย Inno Setup
□ ได้ POS-Monitor-Setup.exe
□ ทดสอบติดตั้งบนเครื่องทดสอบ
□ ตรวจสอบข้อมูลใน Dashboard
□ พร้อมแจกจ่าย!
```

---

## 🎉 สรุป

Windows Installer ทำให้:
- ✅ **ติดตั้งง่าย** - แค่ Next > Next > Finish
- ✅ **มืออาชีพ** - เหมือน Software ทั่วไป
- ✅ **Uninstall ได้** - ผ่าน Control Panel
- ✅ **ไม่ต้องคัดลอกหลายไฟล์** - ไฟล์เดียวจบ

**ขนาดไฟล์:** ~15-20 MB

**เวลาติดตั้ง:** 1-2 นาที

**Happy Installing!** 🚀
