# 🗄️ Auto Archive System - Setup Guide

## ภาพรวมระบบ

ระบบ Auto Archive จะแบ่งข้อมูลออกเป็น 2 Sheet:

```
📊 Google Sheets
├── Current (ข้อมูล 30 วันล่าสุด)    ← Dashboard ดึงจากนี่ (เร็ว)
└── Archive (ข้อมูลทั้งหมด)          ← เก็บประวัติย้อนหลัง (ไม่จำกัด)
```

### ✨ ข้อดี
- ✅ **Dashboard โหลดเร็ว** - ดึงแค่ 30 วันล่าสุด
- ✅ **เก็บข้อมูลไม่จำกัด** - ประวัติทั้งหมดอยู่ใน Archive
- ✅ **อัตโนมัติ 100%** - ย้ายข้อมูลเองทุกวัน เวลา 02:00 น.
- ✅ **ส่งรายงาน Email** - แจ้งผลการ Archive ทุกวัน

---

## 🚀 การติดตั้ง (5 ขั้นตอน)

### ขั้นที่ 1: เปิด Google Sheets

1. เปิด Google Sheets ที่ใช้เก็บข้อมูล POS Monitor
2. ไปที่ **Extensions > Apps Script**

---

### ขั้นที่ 2: แทนที่โค้ดเดิม

1. **ลบโค้ดเดิมทั้งหมด** ใน `Code.gs`
2. **คัดลอกโค้ดจากไฟล์** `Code-Complete.gs`
3. **วางลงใน** Apps Script Editor
4. กด **💾 Save**

---

### ขั้นที่ 3: ตั้งค่า Sheets

1. ใน Apps Script Editor เลือกฟังก์ชัน `setupSheets`
2. กด **▶️ Run**
3. อนุญาตสิทธิ์ตามที่ขอ
4. ตรวจสอบ Logs ว่าเห็นข้อความ:
   ```
   ✅ Sheets setup complete!
   📊 Current sheet: Current
   🗄️ Archive sheet: Archive
   ```

5. **กลับไปดู Google Sheets** จะมี 2 Sheets:
   - ✅ `Current` (ข้อมูลปัจจุบัน)
   - ✅ `Archive` (ข้อมูลเก่า)

---

### ขั้นที่ 4: ตั้งค่า Auto-Archive (Time Trigger)

1. ใน Apps Script Editor เลือกฟังก์ชัน `createDailyTrigger`
2. กด **▶️ Run**
3. ตรวจสอบ Logs ว่าเห็น:
   ```
   ✅ Daily trigger created! Archive will run every day at 2:00 AM
   ```

4. **ตรวจสอบ Trigger**:
   - คลิกไอคอน ⏰ **Triggers** (ซ้ายมือ)
   - ควรเห็น Trigger:
     ```
     Function: autoArchive
     Event: Time-driven
     Time: Every day 2-3am
     ```

---

### ขั้นที่ 5: Deploy Web App (ใหม่)

⚠️ **สำคัญ**: ต้อง Deploy ใหม่เพราะโค้ดเปลี่ยน

1. กด **Deploy > Manage deployments**
2. คลิกไอคอน ✏️ **Edit** ที่ deployment เดิม
3. เปลี่ยน **Version** เป็น **New version**
4. กด **Deploy**
5. **คัดลอก URL ใหม่** (ถ้า URL เปลี่ยน)

---

## ✅ การทดสอบ

### ทดสอบ 1: ทดสอบรับข้อมูล (doPost)

```javascript
// เลือกฟังก์ชัน testDoPost แล้วกด Run
```

ตรวจสอบ:
- ✅ มีข้อมูลเพิ่มใน Sheet `Current`
- ✅ Logs แสดง `"status": "success"`

---

### ทดสอบ 2: ทดสอบส่งข้อมูลไปยัง Dashboard (doGet)

```javascript
// เลือกฟังก์ชัน testDoGet แล้วกด Run
```

ตรวจสอบ:
- ✅ Logs แสดง `Success: true`
- ✅ มีจำนวน Branch ถูกต้อง

---

### ทดสอบ 3: ทดสอบ Auto Archive

```javascript
// เลือกฟังก์ชัน testArchive แล้วกด Run
```

ตรวจสอบ:
- ✅ ข้อมูลเก่ากว่า 30 วันถูกย้ายไป `Archive`
- ✅ ข้อมูล 30 วันล่าสุดอยู่ใน `Current`
- ✅ ได้รับ Email แจ้งผลการ Archive

---

## 📊 การทำงานของระบบ

### 🔄 ทุกวันเวลา 02:00 น.

ระบบจะทำงานอัตโนมัติ:

1. **คำนวณวันตัดรอบ** (30 วันที่แล้ว)
2. **แยกข้อมูล**:
   - ข้อมูลเก่า → ย้ายไป `Archive`
   - ข้อมูลใหม่ → เก็บไว้ใน `Current`
3. **ส่ง Email รายงาน** ให้คุณทราบ

### 📧 Email รายงานจะมีหน้าตา:

```
📊 POS Monitor: Daily Archive Report

Auto-archive completed successfully!

📅 Cutoff Date: 24/11/2024 02:00:00
🗄️ Rows Archived: 4,800
📊 Rows Kept (Current): 2,880
⏰ Time: 24/12/2024 02:00:15

All data older than 30 days has been moved to the Archive sheet.
```

---

## 🎯 การใช้งาน

### ดูข้อมูลปัจจุบัน (30 วันล่าสุด)

- เปิด Sheet `Current`
- Dashboard จะดึงข้อมูลจาก Sheet นี้

### ดูประวัติย้อนหลัง (ทั้งหมด)

- เปิด Sheet `Archive`
- มีข้อมูลทั้งหมดตั้งแต่เริ่มใช้งาน

### ค้นหาข้อมูลเก่า

ใน Sheet `Archive`:
1. กด **Ctrl+F** (Windows) หรือ **Cmd+F** (Mac)
2. ค้นหาชื่อสาขาหรือวันที่
3. หรือใช้ **Filter** เพื่อกรองข้อมูล

---

## ⚙️ การปรับแต่ง

### เปลี่ยนระยะเวลาเก็บข้อมูลใน Current

แก้ไขในไฟล์ `Code-Complete.gs` บรรทัดที่ 20:

```javascript
ARCHIVE_AFTER_DAYS: 30,  // เปลี่ยนเป็น 60 ถ้าต้องการเก็บ 60 วัน
```

### เปลี่ยนเวลารัน Auto Archive

แก้ไขในฟังก์ชัน `createDailyTrigger()`:

```javascript
.atHour(2)  // เปลี่ยนเป็น 3 ถ้าต้องการรันเวลา 03:00 น.
```

แล้วรันฟังก์ชัน `createDailyTrigger` ใหม่

---

## 🛠️ ฟังก์ชันเพิ่มเติม

### ดูสถิติการเก็บข้อมูล

```javascript
// เลือกฟังก์ชัน getArchiveStats แล้วกด Run
```

ผลลัพธ์:
```
📊 Archive Statistics:
Current sheet: 2,880 rows
Archive sheet: 145,200 rows
Total data: 148,080 rows
Storage used: 1.48%
```

### Archive ข้อมูลช่วงเวลาที่กำหนด

```javascript
// แก้ไขวันที่ตามต้องการ
archiveByDateRange('2024-01-01', '2024-01-31');
```

### ลบ Trigger ทั้งหมด (ถ้าต้องการหยุดใช้งาน)

```javascript
// เลือกฟังก์ชัน deleteAllTriggers แล้วกด Run
```

---

## 📈 ประมาณการความจุ

### ตัวอย่าง: 50 สาขา

| ระยะเวลา | Current Sheet | Archive Sheet | รวม |
|---------|---------------|---------------|-----|
| 1 เดือน | 4,800 แถว | 0 | 4,800 |
| 3 เดือน | 4,800 แถว | 9,600 | 14,400 |
| 1 ปี | 4,800 แถว | 1,747,200 | 1,752,000 |
| 5 ปี | 4,800 แถว | 8,755,200 | 8,760,000 |

**ความจุเต็ม**: ประมาณ **5-6 ปี** (ก่อนถึง 10 ล้านแถว)

---

## 🐛 Troubleshooting

### ไม่มี Sheet Archive

- รันฟังก์ชัน `setupSheets` อีกครั้ง

### Trigger ไม่ทำงาน

1. ตรวจสอบที่ **Triggers** (⏰)
2. ดูว่ามี Trigger `autoArchive` หรือไม่
3. ถ้าไม่มี รันฟังก์ชัน `createDailyTrigger` ใหม่

### ไม่ได้รับ Email รายงาน

- ตรวจสอบ Spam folder
- Email จะส่งไปที่ Google Account ที่ใช้เปิด Apps Script

### Dashboard ไม่แสดงข้อมูล

1. ตรวจสอบว่า Deploy Web App ใหม่แล้ว
2. ตรวจสอบ URL ใน `dashboard.js` ว่าถูกต้อง
3. ลองรันฟังก์ชัน `testDoGet` ดูว่ามีข้อมูลหรือไม่

---

## 📋 Checklist การติดตั้ง

- [ ] วางโค้ด `Code-Complete.gs` ใน Apps Script
- [ ] รันฟังก์ชัน `setupSheets`
- [ ] ตรวจสอบมี 2 Sheets (Current + Archive)
- [ ] รันฟังก์ชัน `createDailyTrigger`
- [ ] ตรวจสอบมี Trigger ใน ⏰ Triggers
- [ ] Deploy Web App ใหม่
- [ ] ทดสอบ `testDoPost`
- [ ] ทดสอบ `testDoGet`
- [ ] ทดสอบ `testArchive`
- [ ] ตรวจสอบได้รับ Email รายงาน

---

## 🎉 เสร็จสิ้น!

ตอนนี้คุณมีระบบ Auto Archive ที่:
- ✅ เก็บข้อมูลไม่จำกัดเวลา
- ✅ Dashboard โหลดเร็ว (แค่ 30 วัน)
- ✅ ทำงานอัตโนมัติทุกวัน
- ✅ ส่งรายงานให้ทราบ

**ไม่ต้องกังวลเรื่องข้อมูลเต็มอีกต่อไป!** 🚀
